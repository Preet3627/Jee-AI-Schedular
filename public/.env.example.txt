# This is an example environment file.
# Copy this content into a new file named ".env" in the project root.
# Then, fill in your actual credentials.

# -- Database Configuration --
# Your MySQL database host (e.g., localhost or a remote IP/domain)
DB_HOST=
# Your MySQL database username
DB_USER=
# Your MySQL database password
DB_PASSWORD=
# The name of your MySQL database
DB_NAME=

# -- Server & Security --
# The port the backend server will run on (default is 3001)
PORT=3001
# A long, random, and secret string used for signing authentication tokens.
JWT_SECRET=your_super_secret_and_long_jwt_string
# A 32-character secret key for encrypting user data at rest. IMPORTANT: Do not change this after setting it.
ENCRYPTION_KEY=your_super_secret_32_char_key_!!!!

# -- Google Cloud / Gemini --
# Your Google Gemini API Key (can be overridden by users in-app)
API_KEY=
# Your Google OAuth Client ID (for Google Sign-In, Calendar, Drive)
GOOGLE_CLIENT_ID=
# Your Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=

# -- Email (Nodemailer) for verification and password resets (Optional) --
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# -- Nextcloud Integration --
# The base URL of your Nextcloud instance (e.g., https://cloud.example.com)
NEXTCLOUD_URL=

# The public share token for the read-only folder containing STUDY MATERIALS.
NEXTCLOUD_SHARE_TOKEN=
# The password for the public STUDY MATERIAL share, if you have one set. Leave blank if none.
NEXTCLOUD_SHARE_PASSWORD=

# The public share token for the read-only folder containing MUSIC.
NEXTCLOUD_MUSIC_SHARE_TOKEN=
# The password for the public MUSIC share, if you have one set. Leave blank if none.
NEXTCLOUD_MUSIC_SHARE_PASSWORD=